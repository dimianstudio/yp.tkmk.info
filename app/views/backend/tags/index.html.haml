= title("Админка &#8594; Словари &#8594; Тэги")

%div.container
  = admin_menu("dictionaries")
%div.container
  %div.content    
    %h2 Тэги
    - if @tags.total_entries > 0
      %p= "В словаре <strong>#{@tags.total_entries}</strong> #{Russian.p(@tags.total_entries, "тэг", "тэга", "тэгов")}"
      %table#higlight_table
        %colgroup
          %col
          %col{:width => "63%"}
          %col{:width => "7%"}
          %col{:width => "5%"}        
          %col{:width => "10%"}
          %col{:width => "10%"}
        %tbody
          = render :partial => 'tag', :collection => @tags
      = will_paginate(@tags)
    - else
      %p В словаре нет ни одного тэга
    %div.separator
    %div
      %h3 Добавить тэг(-и)
      %p.help Введите тэги через запятую и нажмите Добавить  
      - form_tag backend_tags_path, :method => "post" do
        = text_area  'tag', 'string', :rows => 3
        = submit_tag("Добавить")