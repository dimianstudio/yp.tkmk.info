%div#h-tabs
  %ul
    %li{:id => ("current" if current == "cart")}= link_to "Карточка", org_path(@org)
    %li{:id => ("current" if current == "phones")}= link_to "Телефоны", org_phones_path(@org)
    %li{:id => ("current" if current == "activities")}= link_to "Виды деятельности", org_activities_path(@org)
    %li{:id => ("current" if current == "maps")}= link_to "Карта", org_maps_path(@org)
    %li{:id => ("current" if current == "images")}= link_to "Фото", org_images_path(@org)
    %li{:id => ("current" if current == "rewiews")}= link_to "Отзывы", org_rewiews_path(@org)
    %li{:id => ("current" if current == "tags")}= link_to "Тэги", org_tags_path(@org)
    %li.user_area{:style => "display:none;", :id => ("current" if current == "edit")}= link_to "Редактировать", edit_org_path(@org)
    %li.user_area{:style => "display:none;", :id => ("current" if current == "delete")}= link_to "Удалить", destroy_org_path(@org), :method => "delete", :confirm => "Вы точно хотите сделать запрос на удаление организации?"
    %li.user_area{:style => "display:none;", :id => ("current" if current == "favorites")}= link_to_remote "Добавить в избранное", {:url => org_favorites_path(@org, :format => "js"), :method => "post"}, :class => "right"
%div#h-tabsline