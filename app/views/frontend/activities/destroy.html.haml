- title(h(@org.name) + ' &#8594; Виды деятельности &#8594; Удаление')

%div.container
  %div.content.round-10
    %h2= h(@org.name)
    = org_menu("activities")
    %h3 Удалить вид деятельности
    %p.help Введите краткое сообщение с причиной по которой вы решили удалить этот вид деятельности. После рассмотрения вашего запроса Администратор приймет решение об удалении информации из базы. ВАЖНО!!! Проверьте на предмет правильности свой e-mail адрес, в случае его некоректности запрос рассматриватся не будет.
    %div.remove-info 
      %p.title Данные для удаления
      %p Название организации: #{h(@org.name)}
      %p <strong>Вид деятельности:</strong> #{h(@activity.name)}
    %div 
      %p !!! Обязательно введите причину удаления (в противном случае запрос будет отклонен)  
      - form_tag delete_org_activity_path(:org_id => @org.id, :id => @activity.id), :method => "delete" do
        %div.tip.required{:title => "Обязательно напишите причину удаления"}= text_area 'request', 'msg', :rows => 3
        %dl
          %dt= submit_tag("Удалить", :class => "button")
          %dd= back_link(:back)